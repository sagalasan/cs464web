{{> navbar }}

<div class="container-fluid">
    <div class="container">
        <h1>{{title}}</h1>
        <p>Welcome to {{title}}</p>

        <p>Query: {{query}}</p>

        <p>Rows:<br/>{{rows}}</p>
    </div>
</div>

{{>footer}}