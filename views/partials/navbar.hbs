<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand" href="/">CS 464 Project</a>
            </div>
            <ul class="nav navbar-nav">
                {{#each optionLinks}}
                    <li {{{ this.active }}}><a href="{{ this.href }}">{{ this.name }}</a></li>
                {{/each}}
            </ul>
        </div>
    </div>
</nav>